# Logging
logging:
  level:
    ROOT: info
    at.jku.dke: trace

spring:
  # Database
  datasource:
    url: jdbc:postgresql://localhost:5433/etutor
    username: etutor_admin
    password: secret
  jpa:
    show-sql: true

  # Mail
  mail:
    sender: etutor@dke.uni-linz.ac.at
    host: localhost
    port: 1025
    username:
    password:

jwt:
  token-lifetime: 900
  refresh-token-lifetime: 1500
